[package]
license = "MIT"
name = "plato-vision"
version = "0.1.0"
edition = "2021"
description = "Camera input translated into text scene descriptions for agents"

[dependencies]
serde = { version = "1", features = ["derive"] }
